export class Mortgage {
    amount: number
    rate: number
    period: number

    constructor(amount: number, rate: number, period: number) {
        this.amount = amount
        this.rate = rate
        this.period = period
    }
}
